// main.ts
function greet(name: string): string {
    return `Hello, ${name}! Welcome to the Tony Buzan-style note-taking app.`;
}

// Call the function and log the result to the console
console.log(greet("User"));
